# Listas do terceiro periodo

Esse repositorio contem as listas de questoes passadas pelo professor Anker da UCL da matéria de Algoritmos e estruturas de dados II

# Como rodar projetos com Raylib-cs

## Configurando o Raylib-cs para a questão 2 da lista 0

**Não deve ser necessário; Tente apenas caso hajam erros de dependencias da raylib ao compilar a questão**

1. Abra o terminal na pasta do projeto: `L0/Q02`.
2. Execute o comando:
   ```sh
   dotnet add package Raylib-cs
   ```
   Isso irá instalar a biblioteca Raylib-cs no projeto selecionado.

## Rodando o projeto no VS Code

1. Instale a extensão C# (Microsoft) no VS Code.
2. Clique no arquivo `Program.cs` dentro de `L0/Q02` e use o botão "Run project associated with this file" (Executar) ou pressione `F5` para rodar o projeto.
   - Se aparecer uma opção, escolha rodar o projeto e não apenas o arquivo.

## Rodando o projeto pelo terminal

1. No terminal, navegue até a pasta do projeto:
   ```sh
   cd L0/Q02
   ```
2. Para compilar:
   ```sh
   dotnet build
   ```
3. Para executar:
   ```sh
   dotnet run
   ```

# Colaboradores

- [Gustavo Gabriel](https://github.com/Gustavo-Wayner)
- [Felipe Gambati](https://github.com/haze168)
- [Victor Bento](https://github.com/VictorBentoAndrade)